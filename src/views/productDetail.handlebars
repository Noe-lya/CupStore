<div>
  <a href="/products">← Volver</a>
  <h1>{{product.name}}</h1>
  <p>Categoría: {{product.category}}</p>
  <p>Precio: ${{product.price}}</p>
  <p>Disponible: {{#if product.available}}Sí{{else}}No{{/if}}</p>
</div>
